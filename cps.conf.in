## CPS_WEBSERVER_PORT
## --------------------
## This is the port you will access your CPS site from.  Set this to a port
## number above 1024 not used for any other server on your computer.

%define CPS_WEBSERVER_PORT 80

## CPS_FTPSERVER_PORT
## --------------------
## This is the port you can use to edit your CPS site via FTP.  Set this to
## a port number above 1024 not used for any other server on your computer.
##
## NOTE: FTP is disabled by default; to enable it, uncomment the
##       <ftp-server> section near the end of zope.conf.

%define CPS_FTPSERVER_PORT 8021

## CPS_DAVSERVER_PORT
## --------------------
## This is the port you can use to edit your CPS site via WebDAV.  Set this
## to a port number above 1024 not used for any other server on your computer.
##
## NOTE: WebDAV is disabled by default; to enable it, uncomment the
##       <webdav-source-server> section near the end of zope.conf.

%define CPS_DAVSERVER_PORT 8020

## ZOPE_MANAGEZODB_PORT
## --------------------
## This is the port you should use to access the root of the Zope Object
## Database (ZODB) from the Zope Management Interface (ZMI).  Set this to a
## port number above 1024 not used for any other server on your computer.

%define ZOPE_MANAGEZODB_PORT 8080